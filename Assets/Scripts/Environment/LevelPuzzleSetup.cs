using UnityEngine;

namespace VRCombat.Core
{
    public sealed class LevelPuzzleSetup : MonoBehaviour
    {
        void Start()
        {
            // Retained only for backwards compatibility with older scenes.
            // The opening arena encounter is now authored explicitly via ArenaOpeningEncounter.
        }
    }
}
